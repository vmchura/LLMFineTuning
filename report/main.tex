%--------------------------------------------------
% CPSC 202: Mathematical Tools for Computer Science
% Yale University
%
% Template for Homework Assignments
% by Yiding Hao
%--------------------------------------------------
\documentclass{cpsc202}

% Please put your information here.
\myname{Chura Quispe, Victor Miguel}
\myprofessor{Massana Raurich, Joaquim}
\mysources{Your Sources}
\hwnumber{0}
\lstset{basicstyle=\ttfamily}

% \singlesided % Uncomment to print single-sided.
\graphicspath{{./media/}}
% Start typing your solution here.
\begin{document}

    \section{Description of the process: selected LLM, pre-processing steps, configuration, and main hyperparameters.}

    \subsection{Selected LLM}
    \href{https://huggingface.co/Qwen/Qwen2.5-0.5B-Instruct}{Qwen2.5-0.5B-Instruct}\\
    The main reason to pick that model as a base it is because it is popular and very tiny.

    \subsection{Pre-processing steps}
    According to the documentation of the base model, it is necessary to concatenate que questions and answers, also to add some keywords:\\
    \lstinline!return f"<|im_start|>system\nYou are a helpful assistant.<|im_end|>!\\
    \lstinline!<|im_start|>user\n{row['Question']}<|im_end|>!\\
    \lstinline!<|im_start|>assistant\n{row['Answer']}<|im_end|>\n"!

    \subsection{Configuration}
    The flow has three steps:
    \begin{enumerate}
        \item Training: with the file CustomLLM/TrainLLM.py \\
        Essentially is just the straight implementation from common repositories but adapted to the model chosen.
        \item Evaluate: with the file CustomLLM/EvaluateLLM.py uses $Rouge~Score$, from the abstract of its paper:
        \begin{displayquote}
            ROUGE stands for Recall-Oriented Understudy for Gisting Evaluation. It includes measures to auto-matically determine the quality of a summary by comparing it to other (ideal) summaries created by humans
        \end{displayquote}

        \item Report: with the file CustomLLM/ReportLLM.py \\
        Creates a detailed report for the CustomLLM/finetuned model.
        \item runner: with the file complete\_flow.py \\
        Orchestrates the previous files mentioned.
        \item Custom console scripts to build ollama model, some helpers could be found in ollama\_build/README.md
    \end{enumerate}

    To allow some fast test, just to verify that all configurations are correctly set it up, the file complete\_flow.py allows the parameter -test.
    Once it worked in my local environment, I've run the complete experiment using free GPU in Colab.

    \subsection{Hyperparameters}
    \textbf{LoRA Parameters}
    \begin{itemize}
        \item LORA\_R: 8 \\
        This is the rank of the low-rank matrices used in LoRA adaptation. \\
        Higher values (like 16 or 32) allow for more expressive adaptations but require more memory and computation. \\
        Lower values (like 4 or 8) are more efficient but might limit how much the model can learn. \\
        Impact: Balances the trade-off between adaptation capacity and computational efficiency.
        \item LORA\_ALPHA: 16 \\
        This is the scaling factor for the LoRA updates. \\
        The effective learning rate for LoRA layers is roughly (LORA\_ALPHA/LORA\_R) * LEARNING\_RATE.\\
        Impact: Controls how aggressive the LoRA updates are relative to the base model. \\
        \item LORA\_DROPOUT: 0.05 \\
        Helps prevent overfitting by randomly dropping some neurons during training.
    \end{itemize}
    \textbf{Training Parameters}
    \begin{itemize}
        \item LEARNING\_RATE: 1e-4,
        \item BATCH\_SIZE: 2,
        \item GRADIENT\_ACCUMULATION\_STEP: 10 \\
        Number of forward passes to accumulate gradients before performing a weight update.
        \item MAX\_ITERATIONS: 1000,
    \end{itemize}

    \newpage
    \section{Output of evaluation}
    \begin{center}
        \begin{figure}
            \includegraphics[width=0.8\textwidth]{rouge_metrics_comparison}
            \includegraphics[width=0.8\textwidth]{score_comparison}
            \caption{ROUGE metrics}
            \label{fig:rouge}
        \end{figure}


    \end{center}
    In the figure\ref{fig:rouge} we can see that the fine-tuned model outperforms to the base model.
    Which it is indicative that the all the setup works as expected. \\
    In the second chart we can see that the second question has the highest metric.
    The question and answers:
    \begin{itemize}
        \item How many shifts is the working day divided into?
        \item Expected response: The working day is divided into 3 shifts: morning, afternoon, and night. The shifts are structured to meet operational needs, with most of the work done in the morning and at night.
        \item Response base model: The standard for a ``shift work'' or ``day shift'' can vary significantly depending on the job responsibilities and the organization's policies, but generally speaking, a 40-hour workweek is common in most workplaces. However, some companies may have more flexible schedules that allow employees to take rest periods during their shifts.
        \item Response of the fine-tuned model: The working day is divided into three shifts: morning, afternoon, and night.
    \end{itemize}

    Clearly we can see a valid response from the fine-tuned model.
    And this also proves the correct setup of the Evaluation step.

    \newpage
    \section{Functional and commented code with prerequisites.}
    All code used for all the steps, and even for this report are in the GitHub repository: \href{https://github.com/vmchura/LLMFineTuning}{vmchura/LLMFineTuning}
    Explanation of main modules:
    \begin{enumerate}
        \item CustomLLM/TrainLLM.py performs the actual training, reads the question and answers, loads a base model, writes the fine-tuned model.
        \item CustomLLM/EvaluateLLM.py performs the evaluation using ROUGE score, loads both models, base and trained and compares the outputs with the expected responses.
        \item CustomLLM/ReportLLM.py performs reporting charts and detailed comparison in html format.
        \item models/finetuned\_yyyymmdd\_HHMMSS, the output model by TrainLLM.py
        \item results/yyyymmdd\_HHMMSS, the results using EvaluateLLM.py and ReportLLM.py
        \item completeflow.py, orchestration of the previous scripts.
        \item ollama\_build, files to create ollama model, there are some guides in README.md file but maybe more manual steps are required, better follow official documentation to build ollama model.
        \item KeywordsOfTokenized.ipynb, simple commands to find the keywords of a base model.
        \item report\_questions.ipynb, when the service of ollama is open and both models, the base and fine-tuned are open I use this script to generate the responses of each model.
        \item requirements.txt, python packages needed to execute all code.
    \end{enumerate}
    \newpage
    \section{Questions, answers before fine-tuning.}
    \include{responses_questions/model_A}
    \section{Questions, answers after fine-tuning.}
    \include{responses_questions/model_B}
    \include{chatgptreport}
\end{document}